  <h1>Congreso Artes</h1>

  <div class="py-4">
    <h2>Registro</h2>
    <hr class="w-50" />
  {{!-- method="POST" enctype="multipart/form-data"  " action="/participantes" "--}}
    <form method='POST'enctype='multipart/form-data' action='/participantes' >
      <div class="form-group row w-50 m-auto">
        <div class="form-group col-12 col-sm-6">
          <label>Email</label>
          <input name="email" id="email" class="form-control m-auto" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Nombre</label>
          <input  name="nombre"id="nombre" class="form-control m-auto" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Password</label>
          <input type="password" name="password" id="password" class="form-control m-auto" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Repita la password</label>
          <input type="password" name="passwordRepeat" id="passwordRepeat" class="form-control m-auto" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Años de experiencia</label>
          <input name="anos_experiencia" id="experiencia" class="form-control m-auto" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Especialidad</label>
          <input  name="especialidad" id="especialidad" class="form-control m-auto" />
        </div>
         <div class="form-group col-12 col-sm-12">
          <label>Foto de perfil</label>
           <input id="foto" type="file" name="foto"/> 
        </div> 
      </div>
      <button class="btn btn-info mb-3" type="submit" value="">Registrarme</button>
      
      <p><a href="/login"> Iniciar sesión</a> | <a href="/">Lista participantes</a> </p>
    </form>
  </div>



<script>
  
</script>
{{!-- 
<script>
  $(document).ready(function () {

    $('button').click(async () => {
      const email = $('#email').val();
      const nombre = $('#nombre').val();
      const password = $('#password').val();
      const passwordRepeat = $('#passwordRepeat').val();
      const anos_experiencia = $('#experiencia').val();
      const especialidad = $('#especialidad').val();
      //const fotoPerfil =  $('#fotoPerfil').val().replace(/C:\\fakepath\\/i, '');
      console.log("se apreto submit:", email, nombre, password, anos_experiencia, especialidad)

      //console.log("aquiiiiiiii:", password, passwordRepeat)
      
      if (password === passwordRepeat) {//fotoPerfil
        const payload = { email, nombre, password, anos_experiencia, especialidad }
        try {
          await axios.post('/participantes', payload) // 
          alert("Usuario registrado con éxito")
          //window.location.href = "/registro1" //redirección
        }
        catch ({ response }) {
          const { data } = response // del response extraemos la data
          const { error } = data //de la data extremos error
          alert(error)
        }
      }else{
        alert("las constraseñas no coinciden")
      }
    })
  })

</script> --}}